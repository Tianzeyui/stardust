# Code Agent TODO

## ✅ 已完成

- [x] `workspace_grep` 代码搜索
- [x] `workspace_write_file` / `workspace_append_file` / `workspace_delete_file` / `workspace_create_dir`
- [x] 工作区外操作确认机制
- [x] 系统提示词引导 AI 改完代码跑 CLI 检查
- [x] Git 工具：`git_status` / `git_diff` / `git_diff_staged` / `git_log`
- [x] Git 工具：`git_add` / `git_reset` / `git_commit` / `git_branch` / `git_checkout`
- [x] `git_commit` 需用户确认

## 🟢 diff 可视化

- [ ] AI 修改代码后，聊天中展示 diff
- [ ] 支持 accept/reject 逐块修改
